public enum Terrain
{
    LAND,
    GRASS,
    STONE,
    RIVER,
}

public enum Obstacle
{
    ROCK,
    WOOD,
    NULL,
}

public enum DistanceAlgorithm
{
    MANHATTAN = 0,
    CHEBYSHEV = 1,
    EUCLIDEAN = 2,
}

public enum ShapeType
{
    CIRCLE=0,
    LINE=1,
    CONE=2,
    RECTANGLE=3
}